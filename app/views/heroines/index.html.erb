<h2>All Heroines</h2>

<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= heroine.super_name %>
    </li>
    <br>
    <li>
      <%= link_to(heroine.name, heroines_path(heroine)) %></li>
  <% end %>

  
</ul>
