<h1>Heroine Creation</h1>

<%= form_for @heroine do |f| %>
  Name: <%= f.text_field :name %><br />
  Super Name : <%= f.text_field :super_name %><br />
  
  Select a Power: <%= f.collection_select(:power_id, Power.all, :id, :name) %>
  <%= f.submit %>
<% end %>